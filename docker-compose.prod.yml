services:
  db:
    image: postgis/postgis:17-3.4
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: agahyar
      POSTGRES_USER: agahyar
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agahyar"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - traefik-network

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - traefik-network

  # adminer:
  #   image: adminer:latest
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     ADMINER_DEFAULT_SERVER: db
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   restart: unless-stopped
  #   networks:
  #     - traefik-network
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.adminer.rule=Host(`adminer.${DOMAIN}`)"
  #     - "traefik.http.routers.adminer.entrypoints=websecure"
  #     - "traefik.http.routers.adminer.tls=true"
  #     - "traefik.http.routers.adminer.tls.certResolver=letsencrypt"
  #     - "traefik.http.services.adminer.loadbalancer.server.port=8080"

  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-unknown}
    env_file:
      - .env
    volumes:
      - ./media:/app/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - traefik-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.agahyar.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.agahyar.entrypoints=websecure"
      - "traefik.http.routers.agahyar.tls=true"
      - "traefik.http.routers.agahyar.tls.certResolver=letsencrypt"
      - "traefik.http.routers.agahyar.priority=1"
      - "traefik.http.services.agahyar.loadbalancer.server.port=8000"

  media:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./media:/usr/share/nginx/html/media:ro
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - traefik-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.agahyar-media.rule=Host(`${DOMAIN}`) && PathPrefix(`/media/`)"
      - "traefik.http.routers.agahyar-media.entrypoints=websecure"
      - "traefik.http.routers.agahyar-media.tls=true"
      - "traefik.http.routers.agahyar-media.tls.certResolver=letsencrypt"
      - "traefik.http.routers.agahyar-media.priority=100"
      - "traefik.http.services.agahyar-media.loadbalancer.server.port=80"

networks:
  traefik-network:
    external: true

volumes:
  postgres_data:
  redis_data:
