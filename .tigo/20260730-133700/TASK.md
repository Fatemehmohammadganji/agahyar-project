# Optimize the queries

- STATUS: OPEN
- PRIORITY: 80
- TAGS: database, optimization

Find all the database queries across the application and optimize them for better performance. If something can be done in SQL and is done in Python instead it should change. If some queries can be combined into one, they should be merged.
